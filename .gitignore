# runtime state, per machine
config.json
state/
captures/

# python — .python is the private interpreter the bootstrap installs when the
# machine has none, .venv is where it puts the packages
__pycache__/
*.py[cod]
.python/
.venv/
venv/
*.egg-info/

# editors / os
.vscode/
.idea/
Thumbs.db
desktop.ini
